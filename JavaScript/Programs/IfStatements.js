function mFunction() {
    var PriceItem=300;
    var PriceProduct=200;

    if(PriceItem == PriceProduct)
    {
        alert("Payement Successfull");
    }
    else
    {
        alert("Payment Unsuccessfull");
    }
}
{
    let a=0;

    if(a>0)
    {
        console.log(`${a} is positive`);
    }
    else if(a==0)
    {
        console.log(`${a} is zero`);
    }
    else
    {
        console.log(`${a} is negative`);
    }
}