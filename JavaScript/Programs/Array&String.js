function mFunction() {

    /*STRING:is the list of characters:

    var text="Hello";
    in backend it is written as: 
    ["H","e","l","l","o"]
    alert(text[0]); for length text.length
    */

    //ARRAYS:

    const products=["Desktop","Computer","Laptop"];
    //if we want to add keyboard at first index:
    //products[0]="Keyboards";
    //using push function to add desktop in list:
    products.push["Desktop"];
    //We want to access all products:
    //alert(products);
    //if we want to access only laptop from the existing list:
    alert(products[products.length-1]);
}